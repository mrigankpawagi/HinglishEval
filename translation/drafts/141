"""Create a function which takes a string representing a file's name, and returns
    'Yes' if the the file's name is valid, and returns 'No' otherwise.
    A file's name is considered to be valid if and only if all the following conditions 
    are met:
    - There should not be more than three digits ('0'-'9') in the file's name.
    - The file's name contains exactly one dot '.'
    - The substring before the dot should not be empty, and it starts with a letter from 
    the english alphapet ('a'-'z' and 'A'-'Z').
    - The substring after the dot should be one of these: ['txt', 'exe', 'dll']
    Examples:
    file_name_check("example.txt") == 'Yes'
    file_name_check("1example.dll") == 'No' (the name should start with a english alphapet letter)
    """

"""
Ek function banao jo file ka naam ek string ke roop me leta hai aur return karta hai
    'Yes' agar file ka naam valid hai, aur 'No' agar valid nahi hai.
    Ek file ka naam tabhi maana jayega jab neeche diye saare gaye conditions pooree ho:
    - File ke naam me teen se zyada digits ('0'-'9') nahi hone chahiye.
    - File ke naam me sirf ek dot '.' hona chahiye.
    - File ka naam me, dot se pehle ka hissa khali nahi hona chahiye, aur uska shuruat english alphabet ('a'-'z' aur 'A'-'Z') se hona chahiye.
    - Dot ke baad ka hissa inme se ek hona chahiye: ['txt', 'exe', 'dll']
    Examples:
    file_name_check("example.txt") == 'Yes'
    file_name_check("1example.dll") == 'No' (naam english alphabet letter se shuru hona chahiye)
"""